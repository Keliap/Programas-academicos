#include<string.h>
#include<iostream>
#include<stdio.h>
using namespace std;

struct prod[3] {
	char nome[30];
	int cod;
	double preco;
};

void imprime(prod P);

main(){
    prod mercado [3];
	for(int i=0; i<3; i++){
	
 	salva

	}
	for(int i=0;i<3; i++)
		imprime(mercado[i]);
}
void imprime(prod){
	cout<<p.nome;
	cout<<p.cod;
	cout<<p.preco;
}
void salva(prod *pontP){
	cout<<"\nInforme o nome:"
	cin>>(*pontP).nome;
	cout<<"\nInforme o código:"
	cin>>pontP->cod;
	cout<<"\nInforme o preço"
	cin>>pontP->preco;
}

