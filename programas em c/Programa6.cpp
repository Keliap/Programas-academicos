#include<stdio.h>
#include <iostream>
#include<math.h>
int tria(int x, int y, int z){
    if (x+y>z && y+z>x && x+z>y)
	printf("Triangulo");
	While(x+y>z && y+z>x && x+z>y)
	
		if(x==y==z)
		printf("Equilatero");
	
	} 
   }
main(){
	int x=0, y=0, z=0;
	tria(x,y,z);
}
